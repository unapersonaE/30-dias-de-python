# Script that prompts the user to enter side a, side b, and side c of the triangle. Calculate the perimeter of the triangle (perimeter = a + b + c).
# Stores the user input as an integer in the 'sideA' variable
sideA = int(input("What's the lenght of the side A of the triangle?"))
# Stores the user input as an integer in the 'sideA' variable
sideB = int(input("What's the lenght of the side B of the triangle?"))
# Stores the user input as an integer in the 'sideA' variable
sideC = int(input("What's the lenght of the side C of the triangle?"))
# Stores the user input as an integer in the 'base' variable
# Calculating perimeter of the triangle
perimeter_of_triangle = (sideA+sideB+sideC)
print('perimeter of triangle is:', perimeter_of_triangle)